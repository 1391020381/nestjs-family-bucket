services:
  nest-app:
    build:
      context: ./
      dockerfile: ./Dockerfile
    depends_on:
      - mysql-container
      - redis-container
    ports:
      - '3000:3000'
  mysql-container:
    image: mysql
    user: root
    ports:
      - '3306:3306'
    volumes:
      - /Users/Volumes/mysql-data:/var/lib/mysql
  redis-container:
    image: redis
    user: root
    ports:
      - '6379:6379'
    volumes:
      - /Users/Volumes/aaa:/data
