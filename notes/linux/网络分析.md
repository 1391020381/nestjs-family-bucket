- ip addr show
- sudo ip link set eth0 up 手动启动网卡
- /etc/netplan/01-netcfg.yaml 静态IP
- sudo netplan apply

- ping -c 4 8.8.8.8

- nslookup google.com
- dig google.com

- 测试端口连通性(TCP/UDP)

  - nc -zv example.com 443

- curl -I https://www.baiduc.com

- 路由与网关检查

- DNS配置

- cat /etc/resolv.conf

- 连接与端口监控

  - ss -tuln 查看监听端口
  - ss -tn # 查看 TCP 连接

- 抓包

  - sudo tcpdump -i eth0 host 8.8.8.8

- 防火墙

  - UFW （Ubuntu）
  - sudo ufw status verbose
  - iptables（传统）

- 网络服务重启
