docker logs -t -f 92bad4317452 \
| awk '/资源上报[[:space:]]\*-+>/ {print; getline; print}' \

> > resource_report.log

另开一个窗口慢慢看

less resource_report.log / tail -f resource_report.log

vim /资源上报

less

some_command | less

- cat less more
- tail -f
- tail -n

- grep -i 'error' /var/log/syslog
- grep -C 3 'failed' /var/log/auth.log
- grep -E 'error|waring' /var/log/syslog

- awk

- send

- tee

- journalctl（systemd 系统）
- journalctl -u ssh
